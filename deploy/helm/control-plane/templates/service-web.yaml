apiVersion: v1
kind: Service
metadata:
  name: {{ include "control-plane.web.fullname" . }}
  labels:
    {{- include "control-plane.web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "control-plane.web.selectorLabels" . | nindent 4 }}
